import { type GetDoctorsByLocationInput, type Doctor } from '../schema';

export async function getDoctorsByLocation(input: GetDoctorsByLocationInput): Promise<Doctor[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all doctors at a specific location from the database.
  // It should return all doctor records that belong to the specified locationId.
  return [];
}