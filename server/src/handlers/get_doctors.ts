import { type Doctor } from '../schema';

export async function getDoctors(): Promise<Doctor[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all doctors from the database.
  // It should return all records from the doctors table with their location information.
  return [];
}